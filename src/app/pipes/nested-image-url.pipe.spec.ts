import { NestedImageUrlPipe } from './nested-image-url.pipe';

describe('NestedImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
